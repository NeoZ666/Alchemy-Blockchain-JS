function startsWithX(string) {
    return (string[0].toLowerCase() === "x") ? true : false;
}

module.exports = startsWithX;

function startsWithX(string) {
    return (string[0].toLowerCase() === "x") ? true : false;
}

module.exports = startsWithX;
