function isAllX(string) {
    let bool = true;
    for(let i=0;i<string.length;i++) {
        if(string[i].toLowerCase() !== 'x') {
            bool = false;
            break;
        }
        else {
            bool = true;
        }
    }
    return bool;
}

module.exports = isAllX;
